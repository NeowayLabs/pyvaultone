from setuptools import setup, find_packages
setup(
    name="pyvaultone",
    version="1.0.0",
    description=("Wrapper for Vault One API"),
    packages=find_packages(),
    include_package_data=True
)
